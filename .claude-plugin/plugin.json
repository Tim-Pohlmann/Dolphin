{
  "name": "dolphin",
  "version": "0.1.0",
  "description": "Custom static analysis powered by Opengrep. Generate rules with Claude, run them in CI.",
  "author": { "name": "Tim Pohlmann" },
  "lspServers": {
    "opengrep-rules": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/launcher/lsp.js"],
      "extensionToLanguage": {
        ".yaml": "yaml",
        ".yml": "yaml"
      }
    }
  }
}
