{
  "name": "dolphin",
  "version": "1.0.1",
  "description": "Custom static analysis powered by Opengrep. Generate rules with Claude, run them in CI.",
  "author": { "name": "Tim Pohlmann" },
  "lspServers": {
    "opengrep-rules": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/launcher/launcher.js", "lsp"],
      "extensionToLanguage": {
        ".yaml": "yaml",
        ".yml": "yaml"
      }
    }
  }
}
